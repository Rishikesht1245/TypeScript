### INITIAL STEPS
* npm install -g typescript ts-node
* npm init -y    (init package.json)
* npm install --save-dev typescript ts-node @types/node   (install as dev dependenices)
* npx tsc --init  (create the tsconfig.json file)